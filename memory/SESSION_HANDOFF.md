# 任务交接

更新日期：2026-07-19

- 最新正式版本为 `v1.2.1`，归档在 `releases/v1.2.1/`；`v1.0.0`、`v1.0.1`、`v1.1.0`、`v1.2.0` 历史归档均保留，不得覆盖或删除。
- 当前候选版本为 `1.2.2-beta.1`：新工程的全部芯片/封装默认安排 SWDIO、SWCLK、NRST，并为 Debug、System、Power设置独立颜色。
- 候选 HTML 位于 `outputs/mspm0g3519-pin-planner.html`，大小 719,580 字节，SHA-256 为 `DBD5B85D44BB95AFFA2C59471C6F2AB5DD58EECD6D3DDBDD2A652941368D54C3`。
- 全部 10 个芯片/封装组合的官方默认物理脚号已自动校验；3507 的 SWDIO改回 PA19、NRST清除，以及3519 的 NRST改为 WAKE并重启恢复均通过。
- v3/v4 和“天猛星”旧 JSON导入不会自动补默认项；VQFN、`PB1` 精确搜索、导入焦点、长标签、打印及完整发布工作流回归通过。
- 未完成事项：等待用户确认 `1.2.2-beta.1` HTML；确认后再生成并验证快速启动文件夹版，不生成便携 EXE或正式版本，除非用户再次明确要求。
