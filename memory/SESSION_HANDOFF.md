# 任务交接

更新日期：2026-08-23

## 当前门禁

- `1.3.0-beta.11` 已将工程选择与所有顶部操作合并到同一排，并删除标题下方的说明小字。
- 每个工程固定为创建时选择的一个芯片型号和一个封装；顶部切换控件已删除。新建工程支持空白工程和动态板卡模板，首次启动没有工程时不能关闭新建窗口。
- 工程存储为 workspace v7/project data v6 单目标结构；旧预发布本地存储和 JSON 按用户要求不读取、不迁移，旧存储键不主动删除。
- `beta.8` 的板卡资源用户文字保护和 `beta.9` 的四种 CSV 公式保护继续有效。

## 下一步

1. 打开当前离线 HTML，确认工程选择、工程菜单、撤销/重做、导入、导出、检查、关于和清空操作位于同一排，标题下没有说明小字。
2. 在常用桌面窗口宽度下确认顶部按钮不会被推出视口；窄窗口允许工具按钮自然换行。
3. 用户确认 HTML 后继续网页版流程；只有用户明确说“发布”后，才改为正式 `1.3.0` 并运行 `create-release.cmd`。

## 未完成事项

- 本轮未启动 Electron；尚未生成 `1.3.0` 正式版本。在用户明确说“发布”前不生成文件夹版或便携 EXE。
