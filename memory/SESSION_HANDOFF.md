# 任务交接

更新日期：2026-08-23

## 当前门禁

- `1.3.0-beta.7` 前端内部整理已完成：保持原生 JavaScript、现有界面、workspace v6/project v5、Electron 桥接和单文件离线交付不变。
- 当前离线候选已由 `build-web.cmd` 重新生成，正在等待用户目视检查界面、工程切换、搜索、引脚编辑和板卡资源启停。

## 下一步

1. 用户反馈视觉或操作问题时，只修改对应 `web/app/` 源码并重新运行 `build-web.cmd`。
2. 用户确认 HTML 后继续保持网页版流程，不自动生成文件夹版或便携 EXE。
3. 只有用户明确说“发布”后，才改为正式 `1.3.0`，按 `docs/RELEASE_CHECKLIST.md` 运行一次 `create-release.cmd` 并创建 `v1.3.0` 标签。

## 未完成事项

- 尚未生成 `1.3.0` 正式版本；在用户明确说“发布”前保持网页版 HTML 工作流。
- 已知的板卡资源关闭时可能连同同条安排中的用户文字一起删除，以及 CSV 公式文字风险，记录在 `memory/KNOWN_ISSUES.md`，未与本次内部重构混改。
