# 任务交接

更新日期：2026-08-23

## 当前门禁

- `1.3.0-beta.9` 已为普通引脚、外设分组、连接器和 KiCad 四种 CSV 导出增加公式文字保护。
- 危险开头只在 CSV 输出时转成普通文本；工程、JSON、workspace v6/project v5、界面和 Electron 桥接均未改变。
- `beta.8` 的板卡资源用户文字保护和共享 SPI1 释放规则继续有效。

## 下一步

1. 在标签、连接器或备注中分别输入以 `=、+、-、@` 开头的文字，导出 CSV 后用常用表格软件打开，确认显示为文字而未计算或执行。
2. 抽查普通引脚、外设分组、连接器和 KiCad 四种 CSV；原始 JSON 中仍应保留未添加文本标记的用户输入。
3. 用户确认 HTML 后继续网页版流程；只有用户明确说“发布”后，才改为正式 `1.3.0` 并运行 `create-release.cmd`。

## 未完成事项

- 尚未生成 `1.3.0` 正式版本；在用户明确说“发布”前不生成文件夹版或便携 EXE。
