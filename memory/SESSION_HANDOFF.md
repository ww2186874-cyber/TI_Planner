# 任务交接

更新日期：2026-08-23

## 当前门禁

- `1.3.0-beta.12` 已将外设实例的信号选择移到外设列表右侧的独立详情栏；展开时画布真实变窄并临时适配，关闭后恢复原视图，不使用覆盖浮层。
- 顶部工程选择与所有操作保持同一排，标题下方的说明小字已删除。
- 每个工程固定为创建时选择的一个芯片型号和一个封装；新建工程支持空白工程和动态板卡模板，首次启动没有工程时不能关闭新建窗口。
- 工程存储为 workspace v7/project data v6 单目标结构；旧预发布本地存储和 JSON 按用户要求不读取、不迁移，旧存储键不主动删除。
- `beta.8` 的板卡资源用户文字保护和 `beta.9` 的四种 CSV 公式保护继续有效。

## 下一步

1. 打开当前离线 HTML，进入“外设资源”并选择 UART0、I2C0、SPI0 等实例，确认信号详情在实例列表右侧展开且不会覆盖芯片。
2. 确认展开时芯片画布向右缩窄并保持完整可见，切换实例不反复跳动；关闭、再次点击当前实例或切回“引脚”后，详情栏收起并恢复原视图。
3. 在常用桌面宽度下抽查滚轮缩放、右键拖动、信号候选高亮和点击候选引脚安排功能。
4. 用户确认 HTML 后继续网页版流程；只有用户明确说“发布”后，才改为正式 `1.3.0` 并运行 `create-release.cmd`。

## 未完成事项

- 本轮未启动 Electron；尚未生成 `1.3.0` 正式版本。在用户明确说“发布”前不生成文件夹版或便携 EXE。
