# 任务交接

更新日期：2026-07-19

- 最新正式版本为 `v1.1.0`，归档在 `releases/v1.1.0/`，不得覆盖或删除。
- 当前候选版本为 `1.2.0-beta.1`，为 MSPM0G3507 和 MSPM0G3519 增加 RHB-32、RGZ-48 VQFN。
- 候选 HTML 位于 `outputs/mspm0g3519-pin-planner.html`；数据校验、旧封装无回归比对、生成可复现性和 HTML 构建已通过。
- 未完成事项：等待用户确认 HTML；确认后运行 `build-folder.cmd`，启动文件夹版并执行 Electron 保存恢复、v3/v4 导入和 VQFN 封装烟雾测试。
- 尚未生成 `1.2.0-beta.1` 文件夹版或便携 EXE，未创建正式发布目录或 Git 标签。
