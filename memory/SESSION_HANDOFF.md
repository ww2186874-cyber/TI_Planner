# 任务交接

更新日期：2026-07-14

- 最新正式版本仍为 `v1.0.1`，不得覆盖或删除。
- `1.1.0-beta.2` 八项增强和左侧滚动条修正已经实现并通过候选测试，等待用户检查 HTML 和快速启动文件夹版。
- 候选产物位于 `outputs/mspm0g3519-pin-planner.html` 和 `outputs/MSPM0-Pin-Planner-1.1.0-beta.2-Folder/`。
- 用户确认后：把版本改为 `1.1.0`，重跑全部构建与检查，执行 `create-release.cmd`，提交正式发布记录并创建 `v1.1.0` 标签。
- 用户未确认前：可以修复候选问题并提升为 `1.1.0-beta.3`，不要创建正式标签或覆盖历史发行版。
