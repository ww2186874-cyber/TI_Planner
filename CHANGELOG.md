# 更新记录

## Harness 插件 0.1.4 - 2026-08-24

- “引脚规划”标签激活时通过官方 `conversation.composer` chain takeover 隐藏整个底部组，包括 todo/goal/queue dock、消息输入卡和统计状态行；切回“对话”或“轨迹”后立即恢复。
- 规划视图不再为已隐藏的 Composer 预留 `--dsh-composer-height` 空间，可使用完整剩余高度；待处理的人类提问、批准或只读提示仍由 Harness 原生 takeover 优先显示，避免阻断必要交互。

## Harness 插件 0.1.3 - 2026-08-24

- “引脚规划”页面改为按 Session 的 Agent Preset 精确挂载：只有 `mspm0-planner` 预设显示该标签，`standard`、`code`、`minimal` 及其他预设不再显示。
- 使用官方 `useSessions` 快照和 Session-scoped Slot 注册；切换到其他预设会话时自动清理页面贡献，切回目标会话时重新挂载，不修改 DSH 本体或内置 Preset。

## Harness 插件 0.1.2 - 2026-08-24

- 恢复原版画布的滚轮连续缩放：缩放范围为 35%–180%，以当前指针位置为锚点，网格同步缩放和平移，并兼容像素、行和页面三种滚轮单位。
- 恢复右键按住拖动画布：使用 Pointer Capture 保持越界拖动，阻止浏览器右键菜单，异常丢失按键时自动结束拖动；拖动引脚起点不会触发选择或安排。
- “适配”模式会随插件分栏尺寸变化重新居中；用户手动缩放或拖动后保留当前视图，重新点击“适配”才恢复自动跟随。

## Harness 插件 0.1.1 - 2026-08-24

- 根据重启后的真实 Harness 截图重做规划标签：恢复原版桌面 EDA 工作台式分栏、方形 LQFP 封装、焊盘与外部功能双层标签、板卡/候选状态色和聚焦式属性检查器。
- 外设实例与官方信号改为相邻分栏，代码和规划检查移入独立覆盖层；画布支持自动适配与缩放，并为 Harness 底部输入栏预留真实高度。
- 使用插件容器查询适配宽屏、中宽与窄内容区，增加封装四边顺序、64 引脚双层 DOM、候选唯一性和 scoped 响应式样式回归测试。
- 代码预览请求增加 Host revision 门禁和返回后状态复核，拒绝展示并发更新后的旧代码；修复普通非候选引脚点击崩溃，并为确认/代码弹窗补齐 Composer 避让、焦点圈定与 Esc 关闭。

## Harness 插件 0.1.0 - 2026-08-24

- 新增独立可插拔的 DeepSeek Harness MSPM0G3519 天猛星 PM-64 规划标签、每会话 Settings Service 和 7 个 AI 工具，不改变现有原生 JavaScript 网页规划器。
- 浏览器与 Host 使用 context-scoped Typert Remote；AI 风险安排必须由当前人类通过 Harness 确认，自动规划保守避开板载、可选外接和电气风险资源。
- 新增 TI SDK 官方头文件来源锁、812 项 IOMUX 映射全量校验，以及只读 DriverLib IOMUX 代码预览；SPI 明确仅为三线基线，固定/非 IOMUX 项不会静默省略。
- 提供本地链接安装、精确 DSH `0.1.1-rc.2` 兼容检查、用户 Agent Preset 接入和安全卸载流程；所有 DSH 重启仍由用户本人执行。

## 1.3.0-beta.13 - 2026-08-23

- 顶部工程选择组在宽屏上向页面中部适度右移，不再紧贴左上角；窗口变窄时会自动收回偏移，避免挤压右侧操作按钮。
- 增加工程选择组宽屏偏移和窄屏复位的模板回归检查。

## 1.3.0-beta.12 - 2026-08-23

- 选择 UART、I2C、SPI 等外设实例后，其官方信号列表改为在外设列表右侧展开独立详情栏，不再插入左侧列表上方。
- 外设详情栏参与正常分栏布局，展开时压缩并临时适配芯片画布，关闭后恢复原画布视图；详情栏不会覆盖芯片。
- 增加外设详情开合、响应式宽度、实例/信号选择和桌面几何烟雾检查。

## 1.3.0-beta.11 - 2026-08-23

- 将工程选择、工程菜单、撤销/重做和导入导出等操作合并到同一排，减少顶部栏占用的垂直空间。
- 删除应用标题下方的工具说明小字，免责声明和数据来源仍保留在页面底部与关于窗口中。

## 1.3.0-beta.10 - 2026-08-23

- 每个工程在创建时固定一个芯片型号和一个封装，工程创建后不能切换目标；顶部型号和封装下拉框已删除。
- 新建工程窗口增加模板、芯片型号和封装选择；开发板模板会自动固定对应目标，首次启动必须先创建工程。
- 工程数据简化为单一目标，workspace 升级到 v7、project data 升级到 v6；软件尚未投入使用，因此不读取旧预发布工作区或 JSON。

## 1.3.0-beta.9 - 2026-08-23

- CSV 中以 `=、+、-、@` 开头或前置隐藏空白后出现这些符号的用户文字，现在会按普通文本导出，避免被表格软件当作公式执行。
- 普通引脚、外设分组、连接器和 KiCad 四种 CSV 导出统一使用同一保护规则，不改变工程和 JSON 中保存的原始文字。
- 增加四种 CSV 的危险文字回归检查。

## 1.3.0-beta.8 - 2026-08-23

- 关闭板卡资源时只释放该资源的预设功能，继续保留用户填写的标签、连接器和备注。
- Flash 与外接 H8 LCD/OLED 共用 SPI1 时，关闭其中一个资源不会释放另一个仍在使用的共享信号；关闭最后一个资源后仍保留用户文字。
- 增加板卡资源关闭、共享总线和用户文字保护回归检查。

## 1.2.2 - 2026-07-19

- 新工程在每种芯片和封装中预先安排官方默认的 SWDIO、SWCLK 和 NRST，同时保留后续修改与清除能力。
- 为下载调试接口和系统引脚增加独立配色，并分别显示 GPIO/复用引脚、系统引脚和电源/地数量。
- 保持已有工程和导入工程原状，不自动覆盖用户已经保存的引脚安排。

## 1.2.2-beta.1 - 2026-07-19

- 新工程在每种芯片和封装中预先安排官方默认的 SWDIO、SWCLK 和 NRST，同时保留后续修改与清除能力。
- 为下载调试接口和系统引脚增加独立配色，并分别显示 GPIO/复用引脚、系统引脚和电源/地数量。
- 保持已有工程和导入工程原状，不自动覆盖用户已经保存的引脚安排。

## 1.2.1 - 2026-07-19

- 修正封装图下边长功能名和自定义标签向上侵入引脚格的问题。
- 修正桌面版导入 JSON 并关闭文件选择/提示框后，搜索和文本输入必须重启才能恢复的问题。

## 1.2.1-beta.1 - 2026-07-19

- 修正封装图下边引脚的长功能名和自定义标签向上侵入引脚格的问题。
- 修正桌面版导入 JSON并关闭文件选择/提示框后，搜索和文本输入必须重启才能恢复的问题。

## 1.2.0 - 2026-07-19

- 正式支持 MSPM0G3507 和 MSPM0G3519 的官方 RHB-32、RGZ-48 VQFN 封装。
- 按官方数据手册校验 VQFN 物理脚序、同芯片复用功能和裸露散热焊盘处理。
- 保持旧 LQFP 数据与 v3/v4 工程兼容，旧工程自动获得独立的空 VQFN 规划状态。
- 修正 GPIO 引脚搜索的前缀误匹配，`PB1` 不再同时命中 `PB10/PB11/PB12`。

## 1.2.0-beta.1 - 2026-07-19

- 为 MSPM0G3507 和 MSPM0G3519 增加官方 RHB-32、RGZ-48 VQFN 封装。
- RGZ-48 复用同芯片 PT-48 引脚数据，RHB-32 按官方物理脚序从同芯片功能数据独立生成。
- 增加 VQFN 引脚映射、跨封装功能一致性和旧工程兼容校验。
- 将界面中的 LQFP 专用文案改为通用封装文案。

## 1.1.0 - 2026-07-14

- 正式发布多工程、撤销/重做、外设完整性、规划检查和扩展导出。
- 正式发布 v4 工作区迁移、数据自动校验、非官方声明和协会作者署名。
- 同一版本同时归档单文件便携版、快速启动文件夹版和离线 HTML。

## 1.1.0-beta.2 - 2026-07-14

- 移除左侧“信号类别”的独立滚动条，统一使用左侧面板整体滚动。
- 复测文件夹版普通引脚的自定义标签、连接器和备注输入。
- 作者署名统一改为“天津职业技术师范大学 电子创新协会”。

## 1.1.0-beta.1 - 2026-07-14

- 增加多工程管理，支持新建、重命名、复制、删除和独立保存。
- 增加最多 80 步的工程级撤销与重做。
- 增加 UART、I2C、SPI、CAN 和 QEI 必需信号完整性提示。
- 增加规划检查报告，覆盖重复信号、重复标签、重复连接器、外设缺失信号和调试接口提醒。
- 增加当前工程/全部工程 JSON、外设分组、连接器、KiCad 和打印报告导出。
- 引脚增加独立的连接器/端子字段。
- 增加非 TI 官方声明、关于窗口、数据来源、免费学习版许可和第三方声明。
- 增加引脚数据自动校验和 v3/v4 导入烟雾测试。
- 增加快速启动文件夹版构建，保留单文件便携版发布方式。
- 本地工作区存储升级到 v4，并自动迁移 v1-v3 数据。

## 1.0.1 - 2026-07-14

- 应用图标改为无文字芯片图形，保留引脚和 1 号脚定位圆点。
- 移除可能造成官方关联误解的厂商品牌文字。

## 1.0.0 - 2026-07-14

- 首个 Windows x64 Electron 便携版。
- 支持 MSPM0G3507 与 MSPM0G3519 的 LQFP 封装。
- 支持引脚规划、外设搜索、QEI、自定义标签、冲突检查、导入导出、主题、旋转、缩放和平移。
